<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Theme.MaterialComponents.DayNight.NoActionBar -->
    <!-- Theme.AppCompat.Light.NoActionBar -->
    <style name="Theme.Ais" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorPrimaryVariant</item>
    </style>

    <style name="Theme.Ais.Login" parent="Theme.Ais">
        <item name="android:windowBackground">@drawable/login_bg</item>
    </style>

    <style name="Login_Link">
        <item name="android:textStyle">normal</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:letterSpacing">0</item>
        <item name="android:textColor">@color/blanco</item>
        <item name="android:layout_marginBottom">5dp</item>
    </style>

    <style name="Login_EditText">
        <item name="android:background">@drawable/login_et_bg</item>
        <item name="android:padding">16dp</item>
        <item name="android:textColorHint">@color/login_form_details</item>
        <item name="android:textColor">@color/blanco</item>
    </style>

    <style name="info_button">
        <item name="android:textStyle">normal</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
        <item name="android:background">@drawable/info_btn_bg</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

</resources>